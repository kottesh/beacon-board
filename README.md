# Beacon Board

Beacon Board is a tiny TypeScript status board for solo projects. It summarizes service health checks into a small JSON report.

## Usage

```bash
npm install
npm test
npm run build
npm run dev
```

## Deploy

Releases are tag based. Push a `v*.*.*` tag to run the release workflow.
